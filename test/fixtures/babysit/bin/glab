#!/usr/bin/env bash
F="$FIXTURES/gitlab"
case "$*" in
  *"/discussions?"*)  cat "$F/discussions.ndjson" ;;
  *"/notes?"*)        cat "$F/notes.ndjson" ;;
  *"merge_requests/"*) cat "$F/mr.json" ;;
  *) echo "fake glab: unexpected call: $*" >&2; exit 1 ;;
esac
